/* calculator.c */
int add(int x, int y) { 
	return (x + y);
}

int sub(int x, int y) { 
	return (x – y);
}

int mul(int x, int y) { 
	return (x * y);
}

float div(int x, int y) { 
	return (x / y);
}

